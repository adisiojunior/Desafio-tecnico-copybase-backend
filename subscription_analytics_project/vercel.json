{
    "builds": [{
        "src": "subscription_analytics_project/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "subscription_analytics_project/wsgi.py"
        }
    ]
}